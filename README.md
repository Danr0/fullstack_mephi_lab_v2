# Web_development_docker
## Run
### Docker compose
##### `docker-compose build`
##### `docker-compose up`
### Only api (Without BD)
##### `run onlyweb`

## Description
Simple middle api for burpsuite api

#### Note: key for burp is hardcoded
For change: change `let key=".."` in `routes.js`